
  {{> header}}
  <table class="table table-hover">
    <thead>
      <tr>
        <th>#</th>
        <th>domain</th>
        <th>email</th>
      </tr>
    </thead>
    <tbody>
      {{#each rows}}
        <tr>
          <th scope="row">{{id}}</th>
          <td>{{{name}}}</td>
          <td>{{email}}</td>
        </tr>
      {{/each}}
    </tbody>
  </table>
  <div class="addButton">
    <button type="button" class="btn btn-primary">Add User</button>
  </div>
{{> footer}}
